- unless @participating_in.empty?
  - @participating_in.each do |campaign|
    - campaign.approved_photos.where(user_id: current_user.id).each do |p|
      = render "photos/participating_photo", photo: p, campaign: campaign
- else
  %p You're not participating in any competitions yet! 

:javascript
  $(".participating-photos img[data-toggle]").tooltip({placement: 'bottom'});
